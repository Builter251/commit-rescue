# api/rescue.py 는 표준 라이브러리(urllib)만 사용합니다. 외부 패키지가 없습니다.
#
# 이 파일이 루트가 아니라 api/ 안에 있는 이유:
#   루트에 requirements.txt 가 있으면 Vercel이 프로젝트 전체를 Python 앱으로 보고
#   "No python entrypoint found" 로 배포가 실패합니다.
#   이 프로젝트는 정적 사이트 + api/ 서버리스 함수 구조라서, 파이썬 의존성 선언은
#   함수 폴더 안에 두어야 합니다.
